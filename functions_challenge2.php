<?php 
function calculateArea($width, $height){
    $area = $width*$height;
    return $area;
}
$totalArea = calculateArea(40,10);
echo " The total area is " . $totalArea . " square units."

?>
